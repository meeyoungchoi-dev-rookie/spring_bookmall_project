<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="BookMallDAO">
    	<select resultType="BookMallManager" parameterType="BookMallManager" id="logincheck">
    		select * from "bookmallmanager" where "admin_id" = #{adminId} and "admin_pw" = #{adminPw} 
    	
    	</select>
    </mapper>